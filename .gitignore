# Dependencies
node_modules/

# An older renderer config wrote its entry here; the repo root is the Pages site's
# parent directory, so a committed root index.html would replace the landing page live.
/index.html

# Build output
out/
dist/

# Bundled agent sidecar binaries (build with `bun run build:omp`; >100MB, not committed)
resources/omp
resources/omp.*

# Logs & OS cruft
*.log
.DS_Store
tmp/

# electron-builder trial/override configs
electron-builder.trial.yml
dev-app-update.yml

# Desktop functional audit artifacts
playwright-report/
test-results/
